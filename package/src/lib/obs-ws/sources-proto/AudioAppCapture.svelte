// Get list of audio devices
// Select audio device
// Audio track selection (show which track is streamed and which is twitch vod (if enabled))
// Audio track volume (can go from -100db to 0db and above 0db up to 26db)
// Audio track mute
